export enum ResponseDataType {
    STRING = 'STRING',
    INTEGER = 'INTEGER',
    FLOAT = 'FLOAT',
    BOOLEAN = 'BOOLEAN',
    DATE = 'DATE',
    TIME = 'TIME',
    DATETIME = 'DATETIME',
    JSON = 'JSON',
    BLOB = 'BLOB'
}
